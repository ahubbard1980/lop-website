---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  /** Overlays a transparent header on top of the page's own hero art
   * (immersive pages like Lore) instead of the opaque bordered bar. */
  floatingHeader?: boolean;
}

const {
  title,
  description = 'Leylines of Power — a trading card game of Nexus Lords and elemental affinities.',
  floatingHeader = false,
} = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700&family=Cinzel+Decorative:wght@700;900&family=Source+Sans+Pro:wght@400&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="site-shell">
      <header class={`site-header${floatingHeader ? ' floating' : ''}`}>
        <a class="brand" href="/">
          <img src="/icons/lop-logo.png" alt="Leylines of Power" width="40" height="40" />
          <span>Leylines of Power</span>
        </a>
        <nav class="site-nav">
          <a class="nav-link" href="/lore">Lore</a>
          <a class="nav-link" href="/rules">Rules</a>
          <a class="play-link btn-gold" href="https://play.leylinesofpower.com">Play Now</a>
        </nav>
      </header>

      <main class="site-main">
        <slot />
      </main>

      <footer class="site-footer">
        <span>&copy; {new Date().getFullYear()} Leylines of Power. In active development.</span>
      </footer>
    </div>
  </body>
</html>

<style>
  .site-shell {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }
  .site-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 16px 24px;
    border-bottom: 1px solid rgba(201, 169, 78, 0.25);
  }
  /* Immersive pages: the header floats transparent over the page's own
     hero art, with a soft top scrim for legibility instead of a bar. */
  .site-header.floating {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    z-index: 10;
    border-bottom: none;
    background: linear-gradient(to bottom, rgba(8, 9, 12, 0.6), rgba(8, 9, 12, 0.25) 60%, transparent);
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    font-family: 'Cinzel Decorative', 'Cinzel', serif;
    font-weight: 700;
    font-size: 17px;
    letter-spacing: 0.5px;
    color: var(--gold-bright);
  }
  .brand img {
    display: block;
    height: 40px;
    width: 40px;
    object-fit: contain;
  }
  .site-nav {
    display: flex;
    align-items: center;
    gap: 20px;
  }
  .nav-link {
    text-decoration: none;
    font-size: 14px;
    font-weight: 600;
    color: var(--fg);
    opacity: 0.8;
  }
  .nav-link:hover {
    opacity: 1;
    color: var(--gold-bright);
  }
  .play-link {
    padding: 10px 20px;
    font-size: 14px;
    box-shadow: 0 4px 14px rgba(205, 169, 79, 0.25);
  }
  .site-main {
    flex: 1;
  }
  .site-footer {
    padding: 16px 24px;
    text-align: center;
    color: var(--muted-fg);
    font-size: 12px;
    border-top: 1px solid rgba(201, 169, 78, 0.15);
  }
</style>
